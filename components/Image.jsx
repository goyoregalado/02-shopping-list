import { View } from "react-native"


const Image = ({}) => {

    const selectImage = (image) => {
        if (type) {
            
        }
    }

    return(
        <View>

        </View>
    )
}